# Demo: quick run (python script)
# 1) Install requirements
#    pip install -r requirements.txt
# 2) Train for a couple of epochs (this will create checkpoints/)
#    python src/train.py --model transformer --epochs 5 --seq_len 48 --batch_size 64
# 3) Evaluate
#    python src/evaluate.py --checkpoint checkpoints/best_model.pt --model transformer
